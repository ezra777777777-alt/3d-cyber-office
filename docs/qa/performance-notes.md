# Performance Notes

## Current Strategy

The app uses Vite vendor chunks and React lazy loading to keep the office shell responsive while dashboard modules load on demand.

## Chunk Groups

- `vendor-3d`: Three.js, React Three Fiber, Drei
- `vendor-react`: React, ReactDOM, Zustand
- App-owned route chunks: produced by Vite and React lazy loading instead of manual `/src` chunk rules.

## Budget

- Initial app chunk target: under 700 kB minified
- Lazy dashboard chunk target: under 250 kB minified
- CSS target: under 80 kB minified
- Vendor chunk target: under 900 kB minified

## Known Tradeoff

The 3D vendor chunk remains large (~870 kB) because Three.js and R3F are core dependencies. The important goal is that dashboard and migration code do not ship inside the first app chunk.

## Plan 15: Visual Density Config

Three visual density presets control scene complexity:

| Mode | Screen Content | Desk Props (keyboard/mug/files + zone props) | Ambient Motion | Decor Items |
|------|---------------|----------------------------------------------|----------------|-------------|
| low | off | off (all desktop clutter removed) | off | 4 |
| normal | on | on | on | 12 |
| showcase | on | on | on | 24 |

The visual mode is persisted in `uiStore` (key: `visualMode`) and can be toggled from the StatusBar.

### Plan 15 Build (2026-05-23)

| Chunk | Size |
|-------|------|
| index.html | 1.12 KB |
| CSS | 28.33 KB |
| runtime | 4.30 KB |
| commander | 40.85 KB |
| dashboard | 52.31 KB |
| index | 66.33 KB |
| vendor-react | 248.10 KB |
| vendor-3d | 869.92 KB |

Index chunk grew from 59.76 KB to 66.33 KB (+6.6 KB) due to new scene components (DeskScreenContent, ProductivityProps, visualDensityConfig). Vendor chunks unchanged.

## Verification

Run:

```powershell
npm.cmd run build
```

Check `dist/assets` and confirm chunks are split by responsibility.

## Plan 33 Release Build Metrics

Generated by:

```powershell
npm.cmd run build
npm.cmd run build:metrics
```

Metrics file: `docs/qa/build-metrics.json`

Policy:

- `vendor-3d` is allowed to be the largest chunk because Three.js/R3F are core.
- App-owned chunks should stay below the configured budgets in `scripts/release/releaseMetrics.mjs`.
- Manual chunks are limited to vendor groups to avoid app-internal circular/dynamic import warnings.
- New build warnings must be either fixed or recorded in `docs/qa/release-smoke-checklist.md`.

Current Plan 33 metrics:

| Group | Max KB | Budget KB | Status |
| --- | ---: | ---: | --- |
| `vendor-3d` | 805.50 | 900 | Within budget |
| `vendor-react` | 242.28 | 280 | Within budget |
| `app` | 146.95 | 220 | Within budget |
| `css` | 35.65 | 90 | Within budget |
| `other` route chunks | 13.53 | 160 | Within budget |

Plan 33.1 stability update:

- `docs/qa/build-metrics.json` no longer stores wall-clock timestamps.
- Vite content hashes are normalized to `[hash]` in committed asset names.
- Running `npm.cmd run build:metrics` twice kept the metrics file hash stable: `C79A10077AEAA3B59F51E701ACCD108B36B086CE567BF2FB7D0ED14C95284BF2`.

## Plan 33 Remaining Build Warnings

Accepted:

- Vite chunk-size warning for `vendor-3d`. The measured `vendor-3d` asset is 805.50 KB, below the configured 900 KB release budget, and contains Three.js/R3F core runtime code.

Resolved:

- The previous dynamic/static import warning for `src/data/demoCommander.ts` was removed by making `CommanderComposer` use the existing static import path.
